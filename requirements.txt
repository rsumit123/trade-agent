# Core
yfinance>=0.2.36
pandas>=2.0.0

# LLM Providers
anthropic>=0.40.0
openai>=1.30.0   # also used for OpenRouter (OpenAI-compatible API)

# Scheduling (for production loop)
apscheduler>=3.10.0

# Dashboard (optional)
fastapi>=0.110.0
uvicorn>=0.27.0
jinja2>=3.1.0

# Timezone support (Python 3.9+)
# zoneinfo is in stdlib for Python 3.9+
# For Python 3.8: pip install backports.zoneinfo
ddgs>=0.1.0

# Session config (YAML-based multi-session support)
pyyaml>=6.0

# Zerodha Kite Connect (real-time NSE data + order placement)
kiteconnect>=5.0.0
pyotp>=2.9.0

# Auth (Google OAuth + JWT + per-user API key encryption)
google-auth>=2.30.0
PyJWT>=2.8.0
cryptography>=42.0.0
